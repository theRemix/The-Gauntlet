package scenes;

import h2d.Graphics;
import h2d.Interactive;

class SimBase extends h2d.Scene{

  private function createProgram(name:String, color:Int, x:Int, y:Int, w:Int, h:Int):Interactive {

    return null;
  }
}
